# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

#GET		/							@controllers.SearchController.index()

POST    /search						@controllers.SearchController.search()
POST    /count						@controllers.SearchController.count()