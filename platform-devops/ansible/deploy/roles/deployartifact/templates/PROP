

consumer-config.xml:

<ConsumerInit serverURI="{{zookeeper_url}}" topic="{{graph_event_topic_id}}"
		ekstepPlatformURI="{{platform_api_url}}"
		ekstepPlatformApiUserId="ilimi">
	</ConsumerInit>

---

database.properties:

db.url=jdbc:mysql://{{mysql_db_url}}/{{mysql_db_schema}}?characterEncoding=UTF-8
db.username={{mysql_db_username}}
db.password={{mysql_db_password}}

hibernate.default_schema={{mysql_db_schema}}

---

elasticsearch.proprties:

elastic-search-host=http://{{search_index_host}}
elastic-search-port={{search_index_port}}

---

graph.properties

environment.id={{environment_id}}

---

language-indexes.properties

elastic-search-host=http://{{language_index_host}}
elastic-search-port={{language_index_port}}

---

mongo.properties

mongo.host=127.0.0.1
mongo.port=27017

---

producer.properties

metadata.broker.list=172.31.1.92:9092
topic=sandbox.learning.graph.events


