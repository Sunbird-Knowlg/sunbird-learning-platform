package org.ekstep.taxonomy.enums;

public class ContentMetadata {

	public static enum ContentDisposition {
		inline, online, attachment
	}
	
	public static enum ContentEncoding {
		gzip, identity
	}
}
