package org.ekstep.dialcode.enums;

/**
 * @author gauraw
 *
 */
public enum DialCodeEnum {
	channel, publisher, count, batchCode, Draft, status, Live, identifier, dialcode, dialcodes, generated_on, published_on, metadata, search, prop_key, prop_value, dialcode_max_index;
}
