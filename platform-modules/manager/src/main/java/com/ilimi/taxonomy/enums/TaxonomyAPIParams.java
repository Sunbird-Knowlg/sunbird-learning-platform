package com.ilimi.taxonomy.enums;

public enum TaxonomyAPIParams {

	taxonomy, taxonomy_hierarchy, concepts, search_criteria, property_keys, unique_constraint, status, Flagged, Live, Draft, isImageObject, node_id;
}
