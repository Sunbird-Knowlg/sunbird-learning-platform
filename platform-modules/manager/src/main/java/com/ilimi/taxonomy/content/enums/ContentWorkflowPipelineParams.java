package com.ilimi.taxonomy.content.enums;

public enum ContentWorkflowPipelineParams {
	element_name, param, data, audioSprite, action, event, manifest, media, theme, group_element_name, controller, events, __cdata;
}
