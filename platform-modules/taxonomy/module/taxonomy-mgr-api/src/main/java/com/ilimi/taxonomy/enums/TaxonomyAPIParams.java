package com.ilimi.taxonomy.enums;

public enum TaxonomyAPIParams {

    taxonomy_id, taxonomy, taxonomy_list, subgraph, search_criteria, definition_node, concepts, concept_hierarchy, concept, metadata_definitions;
}
