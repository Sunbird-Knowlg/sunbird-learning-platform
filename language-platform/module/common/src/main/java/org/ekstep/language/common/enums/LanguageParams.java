package org.ekstep.language.common.enums;

public enum LanguageParams {

    word, text, language_id, complexity_measures, word_complexity, text_complexity, words, texts, file_path, source_type, grade, pos, from_date, to_date, content, synonyms, relations, root_words, word_ids, word_index_info, citation_count, index_info, groupByList, format, input_stream, CSVInputStream, dictionary, synset_stream, word_stream;
}
