redis_backup_dir: /tmp/redis-backup
learner_user: learning
redis_data_dir: /data
redis_version: 6.2.5
redis_dir: "/home/{{ learner_user }}/redis-{{ redis_version }}"

cloud_storage_redisbackup_bucketname: "{{ cloud_storage_management_bucketname }}"
cloud_storage_redisbackup_foldername: redis-backup
